<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Books Repository</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container">
    <%- include('partials/header.ejs') %>

    <div class="container">
      <div class="px-4 py-5 my-5 text-center">
        <img class="d-block mx-auto mb-4" src="<%= book.imageUrl %>" alt="<%= book.title %>" width="172" height="257">
        <h1 class="display-5 fw-bold"><%= book.title %></h1>
        <div class="col-lg-6 mx-auto">
          <p class="lead mb-4 fw-bold">Authors: <%= book.author %></p>
          <p class="lead mb-4 fw-bold">Release Date: <%= book.releaseDate %></p>
          <p class="lead mb-4"><%= book.description %></p>

          <% if (user) { %>
            <p class="text-muted">Usuario logueado: <%= user.email %> - Admin: <%= user.isAdmin ? 'Sí' : 'No' %></p>
          <% } %>

          <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
            <% if (user && user.isAdmin) { %>
              <a href="/books/<%= book.id %>/edit" class="btn btn-primary btn-lg px-4 gap-3">Editar</a>
              <form action="/books/<%= book.id %>/delete" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-outline-secondary btn-lg px-4">Eliminar</button>
              </form>
            <% } %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%- include('partials/footer.ejs') %>
</body>
</html>