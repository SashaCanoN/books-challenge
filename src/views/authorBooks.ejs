<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Libros del Autor</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container">
    <%- include('partials/header.ejs') %>

    <h1 class="text-center my-4">Libros de <%= author?.name || 'Autor desconocido' %></h1>

    <% if (books && books.length > 0) { %>
      <div class="row">
        <% books.forEach(book => { %>
          <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
              <img src="<%= book.imageUrl %>" class="card-img-top" alt="<%= book.title %>" style="height: 300px; object-fit: cover;">
              <div class="card-body">
                <h5 class="card-title"><%= book.title %></h5>
                <p class="card-text"><%= book.description?.substring(0, 100) %>...</p>
                <a href="/books/detail/<%= book.id %>" class="btn btn-outline-primary btn-sm">Ver detalle</a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="alert alert-warning text-center" role="alert">
        Este autor aún no tiene libros registrados.
      </div>
    <% } %>

  </div>

  <%- include('partials/footer.ejs') %>
</body>
</html>
